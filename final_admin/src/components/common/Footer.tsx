import React from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

const CustomFooter: React.FC = () => (
  <Footer style={{ textAlign: 'center' }}>
    Admin Dashboard ©2024 Created by Your Company
  </Footer>
);

export default CustomFooter;
